{% macro projectCard(title, description, imageUrl) %}
<div class="card">
    <a href="{{ projectUrl | url }}"><h3 class="mt-sm card__title mb-sm">{{ title }}</h3></a>
    <img class="card-image mb-md" src="{{ imageUrl | url }}" alt="{{ title }}">
</div>
{% endmacro %}
