{% macro projectCard(title, projectUrl, imageUrl) %}
<div class="card">
    <a href="{{ projectUrl | url }}" target="_blank" title="{{ title }}"><h3 class="mt-sm card__title mb-sm">{{ title }}</h3></a>
    <img loading="lazy" class="card-image mb-md" src="{{ imageUrl | url }}" alt="{{ title }}">
</div>
{% endmacro %}
